{
  "name": "spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "swa:start": "swa start",
    "swa:build": "npm run build && swa start dist --run-from-package",
    "swa:deploy": "npm run build && swa deploy dist"
  },
  "devDependencies": {
    "vite": "^7.0.9"
  },
  "engines": {
    "node": ">=22.2.0"
  }
}
